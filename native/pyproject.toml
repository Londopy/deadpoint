[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "deadpoint-native"
version = "0.1.0"
description = "Optional Rust acceleration for deadpoint."
requires-python = ">=3.10"
license = { text = "MIT" }

[tool.maturin]
module-name = "deadpoint_native"
